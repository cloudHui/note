---
title: warnings
---

# 代码提醒

## 规则

- 统一配置用 bean 获取，其他单例自己 `get`，只用 spring 初始化。
- 表格配置列要放在 `time` 之前，也就是更新时间。
- 拿不到 `player` 时才用统一发送，不然就用玩家身上的 session 发消息。
- 生成代码工具更新只读表时，不要打开更新这种方式。
- 代码合并时要选一下默认提交内容。
- 检查消息发送时机，不要在未登录时触发。
- 临时数据放在 `roleTemp` 上面。
