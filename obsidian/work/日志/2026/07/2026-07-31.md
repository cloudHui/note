```
日志
```

# TODO


- 
- 检查一下目前发消息的地方是否都是放到其他线程去build  而不是心跳线程去做需要修改先检查  然后加日志看下帧数据是否有多余的是否正常
- 世界的战斗 先说要实现类似视野的功能  通过视野去同步转发消息，就是多少范围的人能看到 多少不能看到 然后每个人维护可视列表，每帧维护，然后根局可视列表去同步当前操作消息哪些操作消息 就是一个人能被谁看到 的一个集合  我觉得这样好找一点hash <userid, Set<userId>> 房间内同步需要按这个逻辑同步 每次移动都需要更新被可视列表 还有拖动这个请求也需要更新次级被可视列表。还是九宫格合适呢。九宫格是个什么思路.